# macOS 安裝與疑難排解

琦琦輸入法支援 Apple Silicon Mac 與 macOS 15 以上。M3 Pro 符合硬體需求。安裝包是輸入法，不會像一般 App 一樣點兩下後開啟主視窗；要先把它加入 macOS 的輸入來源。

## 首次安裝

1. 從 [GitHub Releases](https://github.com/polobread/KeyKey/releases) 下載 macOS 的 `*.pkg.zip`，解壓縮後執行 `.pkg`。請使用 Release 附件；Actions 的手動建置 artifact 是未簽章測試包。
2. 安裝完成後，**登出 macOS 並重新登入**，讓系統重新掃描輸入法。
3. 打開「系統設定 → 鍵盤 → 文字輸入 → 編輯 → ＋」。選「繁體中文」，加入「琦琦輸入法」。這是啟用輸入法的步驟。
4. 在畫面右上角的輸入法選單選「琦琦輸入法」。打開「文字編輯」的新文件，輸入一個完整注音音節，確認候選字出現並能選字。琦琦輸入法目前逐字選字；例如「台北」需要先選「台」，再輸入並選「北」。

若是覆蓋安裝新版，正常情況下不必重新登入；遇到選不到輸入法或仍顯示舊版時，重新登入再檢查。

## 截圖中的權限頁與彩色雜訊圖示

「隱私權與安全性 → 裝置控制和資料取用」**不是**加入輸入來源的畫面。該處開關關閉、或圖示看起來異常，不能單獨證明注音輸入無法運作。請先按上面的步驟，在「文字編輯」實際測試。

一般鍵盤輸入走 macOS 的 InputMethodKit。程式另有一段在**用滑鼠點候選字**時送出模擬按鍵的程式碼；如果鍵盤數字鍵可選字、滑鼠點候選字卻沒反應，再檢查該項目是否需要 macOS 的控制權限。只憑圖示無法判定權限、安裝檔或系統圖示快取哪一項出了問題。

## 依症狀檢查

| 症狀 | 先檢查 |
| --- | --- |
| 「＋」清單裡找不到琦琦輸入法 | 確認 `/Library/Input Methods/chichi77 KeyKey.app` 存在；首次安裝後登出再登入，從「繁體中文」清單找。 |
| 找得到，但選單列無法切換到它 | 在「文字輸入 → 編輯」確認已加入；切換到「文字編輯」的可編輯文件再試。 |
| 選得到，但注音或候選字不出現 | 在「文字編輯」試完整音節，記下按鍵後的實際畫面；確認選單列顯示琦琦輸入法，而非 ABC。 |
| 數字鍵選字正常、滑鼠點候選字無效 | 可能涉及模擬按鍵權限；記下 macOS 權限畫面的開關狀態與切換後是否保留。 |
| 只有圖示變成彩色雜訊 | 回報圖示出現的位置，以及輸入功能是否正常；圖示外觀本身不足以判定輸入法故障。 |

## 回報問題時請附上

- 安裝的版本及下載連結，或安裝檔名稱。
- macOS 版本及 build（「關於這台 Mac」可查看）；Mac 晶片型號。
- 「系統設定 → 鍵盤 → 文字輸入 → 編輯」是否有琦琦輸入法，以及選單列是否能選到它。
- 在「文字編輯」輸入一個完整注音音節後的結果；數字鍵與滑鼠點候選字是否各自有效。
- 如果是權限開關問題，請說明打開後是否立刻跳回關閉。

可選用下列唯讀指令查看本機安裝版本與簽章身分：

```sh
/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' '/Library/Input Methods/chichi77 KeyKey.app/Contents/Info.plist'
codesign -dv '/Library/Input Methods/chichi77 KeyKey.app' 2>&1 | grep -E 'Signature=|TeamIdentifier='
```

開發者對照：[安裝包建置與簽章](Installer/README.md)、[macOS functional test](tests/HEART_SUTRA_FUNCTIONAL.md)。
