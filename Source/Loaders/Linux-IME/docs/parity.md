# Linux feature parity ledger

This ledger records evidence; it is not a product-support claim. The macOS
behavior baseline and Linux desktop acceptance remain pending unless an entry
links to concrete evidence. Test IDs refer to `LINUX_TEST_PLAN.md`.

| ID | Scope | macOS source | macOS operation evidence | Linux implementation | Tests | Known difference | Status |
|---|---|---|---|---|---|---|---|
| F01 | Traditional Bopomofo | `OSX-IMK/CVApplicationController.mm`, `OVIMMandarin` | Pending P0 recording | Single-syllable engine and Fcitx 5 adapter vertical slice | L1 real-data tests; local `T01-X11-GTK3-BOPOMOFO-STANDARD` | Basic candidate/edit/cancel path only; no GNOME/Wayland evidence | Partial, local L3 X11 |
| F02 | Five Bopomofo layouts | `OSX/TakaoPhonetic.m`, `TakaoKeyboardLayoutPopUpButton.m` | Pending P0 recording | Standard, ETen, ETen26, Hsu and Hanyu Pinyin parsers; persistent Fcitx-native combo-box setting | L1: real dictionary round trips plus Pinyin cases; local L3 T02 same-syllable typing for all five layouts and config schema | Multi-key ambiguous layouts retain documented collisions; full tone/incomplete-input E2E and macOS baseline pending | Partial, local L3 X11 |
| F03 | Cangjie | `OSX/TakaoCangjie.m`, `OVIMGeneric`, `cj-ext.cin` | Pending P0 recording | CIN key-name preedit, candidate query/selection, five-code limit, Fcitx registration | L1 real-data tests; local `T04-X11-GTK3-CANGJIE` `a` → `日` plus negative control | No dynamic frequency, wildcard, full-code trigger, filters, punctuation, or GNOME/Wayland evidence | Partial, local L3 X11 |
| F04 | Simplex | `OSX/TakaoSimplex.m`, `OVIMGeneric`, `simplex-ext.cin` | Pending P0 recording | Separate CIN dictionary, key-name preedit, candidate query/selection, two-code limit, Fcitx registration | L1 real-data tests; local `T05-X11-GTK3-SIMPLEX` second-candidate `a` → `曰` plus negative control | No full-code trigger, search-as-you-type, filters, punctuation, or GNOME/Wayland evidence | Partial, local L3 X11 |
| F05 | Associated phrases and built-in collections | `OVIMMandarin/OVAFAssociatedPhrase.cpp`, `OSX/TakaoPhraseCollections.m` | Pending P0 recording | Native parser for the McBopomofo base plus 29 category collections; ordered merge/dedup; first-run base default; nested Fcitx-native Boolean setting for every collection with hidden migration of the earlier comma format; headless suffix candidates selected with Shift+1–9; all options off disables associations | L1 parser/real-data/order/filter/keyboard-flow tests; local T07 X11 default `今天`, `history`-only `臺灣史`, disabled, legacy-migration, D-Bus persistence, and AT-SPI config-window click/persistence flows; the last enables `agriculture-food`, restarts Fcitx, and types `作物育種`; running schema exposes 30 Boolean options | No candidate mouse selection, desktop logout/login, GNOME/Wayland, or macOS baseline evidence | Partial, local L3 X11 |
| F06 | Vertical/horizontal candidate UI | `OSX-IMK/CVVerticalCandidateController.mm`, `CVHorizontalCandidateController.mm` | Pending P0 recording | Fcitx-owned candidate list; cyclic Up/Down highlight movement, Left/Right/PageUp/PageDown paging, Enter and numeric selection | L1 navigation boundaries; local `T06-X11-GTK3-CANDIDATE-NAVIGATION` commits `妐` through PageDown → Down → Enter | No custom orientation, mouse E2E, GNOME/Wayland popup, or visual parity yet | Partial, local L3 X11 |
| F07 | Scale and colors | `OSX/TakaoGlobal.m`, candidate controllers | Pending P0 recording | None | T12 pending | None accepted | Not started |
| F08 | General settings and switching | `OSX/TakaoGlobal.m`, `OSX-IMK/OpenVanillaController.mm` | Pending P0 recording | Three Fcitx input-method registrations; persistent Bopomofo layout, Traditional-to-Simplified, and nested associated-collection Boolean settings; automated switching among methods and `keyboard-us` | Local X11 switching in T01/T02/T04/T05 slices; running config schema; AT-SPI config-window click plus INI/D-Bus/process-restart persistence for one collection selection; T10/T12 pending | Desktop logout/login and the remaining final settings UI have not been verified | Partial, local L3 X11 |
| F09 | Full/half width and Traditional-to-Simplified | `OVOFFullWidthCharacter`, `OVOFHanConvert` | Pending P0 recording | Per-input-context full/half-width state toggled by `Shift+Space`; ASCII space and `!`–`~` mapping; native Boolean Traditional-to-Simplified option using the 3,058 explicit legacy mappings; mode changes preserve active reading/candidates | L1 mapping/state/preservation and real-data conversion tests; local `T08-X11-GTK3-FULL-WIDTH` commits exact `Ａ！～　`; local `T08-X11-GTK3-TRADITIONAL-TO-SIMPLIFIED` selects `臺灣` and commits `台湾`; both have literal `keyboard-us` controls | macOS shortcut and combined-filter order baseline pending; no Fcitx inactive-English/global width state, dedicated settings window, GNOME, or Wayland evidence | Partial, local L3 X11 |
| F10 | Bopomofo correction and punctuation | `OVAFBopomofoCorrection`, punctuation CIN data | Pending P0 recording | Loads pinned `bpmf-punctuations.cin`; `Ctrl+,` and `Ctrl+.` commit `，` and `。`; recognized shortcuts preserve an active reading while undefined Ctrl shortcuts pass through | L1 real-data shortcut/list tests; local `T12-X11-GTK3-SYMBOL-LIST` covers the shared table path | No correction table, direct layout punctuation, full T08/T09, or GNOME/Wayland evidence yet | Partial, local L3 X11 |
| F11 | Symbol/emoticon/common-text panel | `OSX-IMK/CVSymbolController.mm`, `CVButtonViewController.mm`, `CVSmileyViewController.mm` | Pending P0 recording | Fcitx-owned punctuation/symbol candidate list opened by `Ctrl+0`/`Ctrl+1`, with paging and keyboard selection | L1 real-data candidate tests; local `T12-X11-GTK3-SYMBOL-LIST` opens `Ctrl+0` and selects `，` | No custom symbol/emoticon/common-text window, mouse/visual, GNOME, or Wayland evidence yet | Partial, local L3 X11 |
| F16 | Settings/about/localization | `OSX/TakaoPreference_Toolbar.m`, `OSX-IMK/CVAboutController.mm` | Pending P0 recording | Fcitx-native five-layout, conversion, and nested 30-checkbox associated-collection settings schema; Traditional Chinese registration names and staged license files | Config schema queried from running installed addon; an AT-SPI-driven `fcitx5-config-qt` test finds named controls, clicks two collection checkboxes and OK, captures PNGs, verifies restart persistence, and affects real X11 typing; T12/T14 pending | Remaining settings, About window, full localization, keyboard-only accessibility flow, GNOME, and Wayland are unverified | Partial, local L3 X11 |

F12–F15 are intentionally excluded by the product decision and are not
missing-parity items. L1 flows call the Linux engine directly and must not be
cited as desktop evidence. The separately named local X11 cases do reach an
installed Fcitx addon and real GTK 3 entry, but cover only the narrow flows
listed above and are not complete T01/T04/T05/T06 or GNOME/Wayland acceptance.
