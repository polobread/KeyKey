# Linux feature parity ledger

This ledger records evidence; it is not a product-support claim. Behavior
investigations inspect the macOS OSX-IMK/PlainVanilla/module flow first, then
compare Windows TSF; phase-one scope remains bounded by the Windows runtime.
Linux desktop acceptance remains pending unless an entry links to concrete
evidence. Test IDs refer to `LINUX_TEST_PLAN.md`.

| ID | Scope | Reference source (macOS first, then Windows) | Behavior/scope evidence | Linux implementation | Tests | Known difference | Status |
|---|---|---|---|---|---|---|---|
| F01 | Traditional Bopomofo | `OSX-IMK/OpenVanillaController.mm`, `OVIMTraditionalMandarin.cpp`, `PlainVanilla/PVLoaderSystem.h`, then `Windows-TSF/KeyKeyEngine.cpp` | macOS source flow inspected first and Windows flow agrees for reading/editing-key ownership; full recordings pending | Single-syllable engine and Fcitx 5 adapter; explicit tones immediately query and commit a sole candidate; optional Big5-HKSCS candidate filter; candidate-to-next-reading continuous input; invalid printable/no-candidate error signal preserves reading; empty/reading/candidate Backspace and Escape boundaries; active-reading navigation/Delete/Tab and Shift variants preserve the reading while Ctrl/Alt/Super shortcuts pass | L1 real-data, representability, continuous-input, error-state and complete edit/cancel anchors; local GTK3, GTK4 and Qt6 X11 clients independently verify ordered Standard preedit, commit, edit/cancel, candidates, associations, modes, context/client/two-App lifecycle, editing fields, symbols and literal negative controls; the error paths traverse the XDG event-sound request without destabilizing Fcitx | Actual audible GNOME output, multi-App active-preedit/edit-cancel and Wayland evidence remain incomplete | Partial, local L1/L3 X11 |
| F02 | Additional Bopomofo layouts | `OVIMTraditionalMandarin.cpp`, then `Windows-TSF/SettingsApp.cpp` | macOS source flow establishes that an invalid printable key is handled with a beep while preserving the reading and combined Ctrl/Alt keys pass through; Windows shares the module behavior and its TSF frontend excludes Ctrl/Alt application shortcuts before dispatch; full recordings pending | ETen, ETen26, Hsu and Hanyu Pinyin parsers with persistent Fcitx-native combo-box setting; shared immediate-tone-query and continuous-input paths | L1 real dictionary round trips, tones, Pinyin, continuous-input, invalid-key and shortcut-preservation checks; local GTK3, GTK4 and Qt6 L3 T02 cases type `麻馬罵嘛` through all five layouts without an extra Space after tones, preserving every layout's active reading across an invalid bare `\` and passed-through `Ctrl+C` | GNOME/Wayland and platform recordings pending | Partial, local L1/L3 X11 |
| F03 | Cangjie | `OSX/TakaoCangjie.m`, `OVIMGeneric`, `cj-ext.cin` | Outside Windows baseline | Existing CIN composition, wildcard, L1 and local X11 vertical slice retained as an extension point | Existing T04 regression | Does not block phase-one Windows parity | Retained extension |
| F04 | Simplex | `OSX/TakaoSimplex.m`, `OVIMGeneric`, `simplex-ext.cin` | Outside Windows baseline | Existing CIN composition, L1 and local X11 vertical slice retained as an extension point | Existing T05 regression | Does not block phase-one Windows parity | Retained extension |
| F05 | Associated phrases and built-in collections | `OVIMMandarin/OVAFAssociatedPhrase.cpp`, then `Windows-TSF/SettingsApp.cpp`, `KeyKeyEngine.cpp` | macOS source commits the head character before showing headless suffixes selected by Shift+1–9; its associated panel does not yield candidate control, so horizontal clicks are disabled and vertical selection changes are rejected. Windows loads the same module and settings into its keyboard-only custom candidate window; full recordings pending | Native parser for the McBopomofo base plus 29 category collections; ordered merge/dedup; first-run base default; nested Fcitx-native Boolean setting for every collection with hidden migration of the earlier comma format; headless suffix candidates selected with Shift+1–9; all options off disables associations; Password/Sensitive capability clears association state; Fcitx's native pointer callback remains an extra Linux behavior, not a parity requirement | L1 parser/real-data/order/filter/keyboard-flow tests; local GTK3, GTK4 and Qt6 T07 X11 cases cover default `今天`, `history`-only `臺灣史`, disabled collections, legacy migration, and D-Bus persistence across Fcitx restart; a GTK3 AT-SPI config-window flow enables `agriculture-food`, restarts Fcitx, and types `作物育種`; running schema exposes 30 Boolean options; T11 confirms all password clients produce literal input without association candidates | Linux associated-candidate pointer behavior, desktop logout/login, GNOME/Wayland, and platform recordings remain unverified | Partial, local L3 X11 |
| F06 | Vertical/horizontal candidate UI | `PlainVanilla/PVCandidate.h`, then `Windows-TSF/CandidateWindow.cpp`, `CandidateStateTest.cpp` | macOS changes the actual panel orientation: Up/Down highlights in vertical mode while Left/Right highlights in horizontal mode, and its controllers convert a clicked row/index to a candidate key; Windows changes custom-window painting only, keeps the core vertical keyboard semantics in both styles, and has no mouse-button selection message; full recordings pending | Fcitx-owned candidate list with native selection callback; persistent Vertical/Horizontal setting applied through `CandidateLayoutHint`; Windows-parity cyclic Up/Down highlight movement, Left/Right/PageUp/PageDown/Space paging, whole-list Home/End, Enter, numeric and pointer selection in both visual styles | L1 navigation boundaries; GTK3, GTK4 and Qt6 vertical T06 cases traverse End/Home before paging and commit `妐`, then wait past transient popup geometries, click the expanded window's second row through XTest, and commit `鐘`; GTK4 and Qt6 horizontal cases prove the Windows key roles remain active; AT-SPI changes Vertical to Horizontal, saves/restarts/reads it back, then completes a real associated-candidate flow; WSLg/GTK3 manual typing works, but its known X11 `UnmapWindow` ghost is excluded from visual acceptance | Pointer selection is a retained Linux/macOS capability absent from the current Windows window; GNOME X11/XWayland/native-Wayland popup geometry, pointer behavior and visual parity remain unverified | Partial, local L3 X11 |
| F07 | Scale and colors | `OSX-IMK/OpenVanillaController.mm`, `CVHorizontalCandidateController.mm`, `CVVerticalCandidateController.mm`, `PreferenceApplications/OSX/TakaoGlobal.m`, then `Windows-TSF/CandidateWindow.cpp`, `SettingsApp.cpp` | Platform source/operation comparison pending | Fcitx provides semantic candidate highlight and delegates actual size/color to the active UI/theme; the input-method addon API has no per-IME scale or highlight-color control | Ubuntu 22.04/24.04 API inventory; T12 visual acceptance pending | Exact Windows 75%–350% and purple/green/yellow/red choices require a custom Fcitx UI addon or an accepted platform difference; no inert settings are exposed | Design required |
| F08 | General settings and switching | `OSX-IMK/OpenVanillaController.mm`, then `Windows-TSF/TextService.cpp`, `LangBarButton.cpp`, `SettingsApp.cpp` | macOS source uses `Ctrl+\` to rotate built-in input methods, has no bare-Shift internal Chinese/English toggle, and exposes full width through Command+Shift+Space; Windows provides the phase-one internal Chinese/English mode, optional `Ctrl+\`, fixed Ctrl+Space, short bare Shift, Shift+Space width toggle and Caps Lock behavior; both pass ordinary application shortcuts before the engine and apply Traditional-to-Simplified conversion at output | Five-layout, Traditional-to-Simplified, all-Unicode candidate, typing-error sound, `Ctrl+\` mode-toggle and nested associated-collection settings; default-enabled error feedback uses the XDG `bell-window-system` event and can be disabled; per-context Chinese/English mode has compact status labels, default `Ctrl+\` and 300 ms bare-Shift actions, composition cancellation, English pass-through and English full-width output; automated switching to Bopomofo and `keyboard-us`; Cangjie/Simplex registrations retained | GTK3, GTK4 and Qt6 T08–T11 X11 flows cover modes, full width, conversion, shortcut/modifier pass-through, two contexts, client/Fcitx restart, two live Apps, exact pointer selection and read-only/password boundaries; running config schema and AT-SPI config-window clicks verify INI/D-Bus/process-restart persistence | Disabled shortcuts and passed-through text use per-client insertion semantics; exact buffers therefore differ across GTK3, GTK4 and Qt6 and are not normalized by the addon. X11 repeat key-down may lack Fcitx Repeat state, so Linux latches the physical backslash until release; Super remains L1-only because Xvfb has no desktop owner. macOS commits its composing buffer on deactivation while Windows TSF abandons composition, so active-preedit focus behavior still requires GNOME/toolkit evidence; audible GNOME confirmation, desktop logout/login and remaining settings are unverified | Partial, local L1/L3 X11 |
| F09 | Full/half width and retained Traditional-to-Simplified extension | `OSX-IMK/OpenVanillaController.mm`, `OVOFFullWidthCharacter`, then `Windows-TSF/TextService.cpp` | macOS exposes its output filter through Command+Shift+Space; Windows owns per-context width state with Shift+Space; both conversion paths transform committed output, while full recordings remain pending | Per-input-context full/half-width state toggled by `Shift+Space`; ASCII space and `!`–`~` mapping; retained Traditional-to-Simplified option using 3,058 explicit legacy mappings; mode changes preserve active reading/candidates | L1 mapping/state/preservation and real-data conversion tests; local GTK3, GTK4 and Qt6 T08 cases commit exact `Ａ！～　`, verify full-width English output, select `臺灣` and commit `台湾`, all with literal `keyboard-us` controls | Inactive/global width state, dedicated settings window, GNOME, and Wayland evidence remain | Partial, local L3 X11 |
| F10 | Bopomofo punctuation | `OVIMTraditionalMandarin.cpp`, punctuation CIN data | Pending P0 recording | Loads pinned `bpmf-punctuations.cin`; `Ctrl+,` and `Ctrl+.` commit `，` and `。`; recognized shortcuts preserve an active reading while undefined Ctrl shortcuts pass through | L1 real-data shortcut/list tests; local GTK3, GTK4 and Qt6 T12 symbol-list cases cover the shared table path | Auto-correction is not in the Windows runtime; direct punctuation across five layouts and GNOME/Wayland evidence remain | Partial, local L3 X11 |
| F11 | Windows symbol list | `OVIMTraditionalMandarin.cpp`, `Windows-TSF/KeyKeyEngine.cpp` | macOS TraditionalMandarin yields the punctuation list to the generic candidate handler, so its controllers permit pointer selection; Windows `Ctrl+0`/`Ctrl+1` opens the same data path but its custom window has no mouse-button selection message; full recordings pending | Fcitx-owned punctuation/symbol candidate list opened by `Ctrl+0`/`Ctrl+1`, with paging, keyboard selection, and retained native pointer selection as an extra Linux capability | L1 real-data candidate tests; local GTK3, GTK4 and Qt6 T12 cases open `Ctrl+0`, select `，` by keyboard, or click the first vertical row and commit exact `，!` versus literal `!` under `keyboard-us` | GNOME X11/XWayland/native-Wayland pointer and visual evidence remain; macOS-only custom emoticon/common-text windows are outside phase one | Partial, local L3 X11 |
| F16 | Settings/about/localization | `PreferenceApplications/OSX`, then `Windows-TSF/SettingsApp.cpp`, `TextService.cpp` | Pending macOS recording and Windows scope/operation cross-check | Schema exposes five layouts, vertical/horizontal candidates, conversion, all-Unicode filtering, typing-error sound, the `Ctrl+\` mode toggle and 30 associated-collection checkboxes; Traditional Chinese registration names and staged license files | Config schema queried from running installed addon; an AT-SPI-driven `fcitx5-config-qt` test changes candidate style to Horizontal, disables error sound and `Ctrl+\`, clicks two collection checkboxes and OK, captures PNGs, verifies restart persistence, and affects real X11 typing; T12/T14 pending | Scale/color design, remaining Windows settings, localization, accessibility, GNOME and Wayland are unverified; a macOS-style About window is not a phase-one blocker | Partial, local L3 X11 |

Phase one uses the Windows TSF runtime as its parity baseline. Bopomofo
learning/dynamic frequency, auto-correction, and F12–F15 are not Windows-parity
items. F03/F04 and the existing Traditional-to-Simplified path remain as
extension features and are not removed. L1 flows call the Linux engine directly and must not be
cited as desktop evidence. The separately named local X11 cases do reach an
installed Fcitx addon and real GTK 3/GTK 4/Qt 6 editors, but cover only the Xvfb
flows listed above and are not GNOME/Wayland acceptance.
